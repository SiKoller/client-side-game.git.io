/**
 * This class was made if i wanted to create more complicated Dialogues 
 * so I am more typesave an with properties instead of using any.
 * But is does not get used yet
 */
export default class Dialogue {
    constructor(prompts) {
      this.prompts = prompts;
    }
  }
  